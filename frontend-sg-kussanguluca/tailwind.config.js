/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      // Aqui podemos adicionar cores personalizadas da tua marca depois
      colors: {
        brand: {
          50: '#f0f9ff',
          500: '#0ea5e9', // Um azul bonito como exemplo primário
          700: '#0369a1',
        }
      },
       animation: {
        'fade-in-up': 'fadeInUp 0.5s ease-out',
      },
      keyframes: {
        fadeInUp: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      }
    },
  },
  plugins: [],
}
